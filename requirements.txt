# Core ML and Data Science
torch>=2.0.0
# torchvision is only needed for vision demos; comment to reduce footprint
# torchvision>=0.15.0
numpy>=1.24.0
# pandas is heavy; keep but allow optional runtime pin via requirements-runtime.txt
pandas>=2.0.0
scikit-learn>=1.3.0

# Natural Language Processing
# Make NLTK optional; only used if enabled in preprocessing
nltk>=3.8.0

# Data Visualization (dev-only; not needed in runtime servers)
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress and Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Web Framework for API
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Development and Testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.5.0

# Jupyter for Exploration
jupyter>=1.0.0
notebook>=7.0.0